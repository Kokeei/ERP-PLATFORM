{
  "name": "erp-platform",
  "private": true,
  "version": "1.0.0",
  "description": "ERP modulaire interne - Prototype et développement.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
